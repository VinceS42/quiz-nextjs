import React from "react";
import Hero from "./component/Hero";

type Props = {};

const Home = (props: Props) => {
    return (
        <>            
            <Hero />
        </>
    );
};

export default Home;
